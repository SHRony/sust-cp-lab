/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['userpic.codeforces.org', 'via.placeholder.com', 'source.unsplash.com', 'c4.wallpaperflare.com'],
  },
  
};

export default nextConfig;
